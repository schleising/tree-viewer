# Tree Viewer

Convert a hierarchical CSV into `graph.json` and view it as an expandable tree of status-coloured nodes.

See the [design document](DESIGN.md) for the JSON contract and [API](api.md) for the Python package.

Usage is in the project [README](../README.md).
